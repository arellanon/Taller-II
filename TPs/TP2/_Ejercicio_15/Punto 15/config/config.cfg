ip_host = 127.0.0.1 #ip del nodo
port_host = 5000 #puerto del nodo
buffer_size = 2048 #tamaño del buffer de lectura (recvfrom)
broadcast = 5 #intervalos de tiempo en los cuales se solicita información a los nodos
tiempo_nodo_activo = 15 #Tiempo a partir del cual un nodo se considera inactivo. La función se ejecuta cada 6 segundos y pregunta si el nodo respondio en un intervalo menor a los 15 segundos(nunca debe ser menor al broadcast).
